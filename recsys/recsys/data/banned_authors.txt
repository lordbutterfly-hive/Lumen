# Globally banned authors — operator list, human-written and human-reviewed.
#
# One Hive account per line. `#` starts a comment. Names are lower-cased and a
# leading `@` is stripped, so `@Name` and `name` are the same entry.
#
# An account here is dropped from EVERY viewer's candidate pool before any lane
# or quota runs, AND its votes/comments/reblogs mint no breadth for anyone else.
# See recsys/core/banned.py for why both halves are required.
#
# This list is never derived, never voted on, and never written by code. Adding
# a name is an operator decision that takes effect on the next service restart.
# `RECSYS_BANNED_AUTHORS` (comma-separated) is unioned with this file for an
# urgent addition without a release.

# 2026-08-08 — owner: persistent troll, banned from Lumen entirely.
kgakakillerg

# 2026-08-23 — RECONCILED WITH THE APP'S `REACT_APP_BANNED_AUTHORS`.
# The two lists had diverged: the Lumen app refused to render six accounts while this
# ranker still counted five of them, so their votes kept shaping every reader's feed. An
# account that cannot be SEEN but can still PROMOTE is a stronger position than it held
# before the ban, which is exactly what `banned.py` says a ban must not leave open.
#
# ONE DIRECTION ONLY: app-ban -> ranker-ban. The reverse merge would be a serious mistake,
# because `curator_accounts.txt` is a different concept ("their engagement mints nothing,
# their own posts rank normally") and folding it into the app's ban list would delete
# curie, ecency, hivebuzz, worldmappin and 17 others from the site.
#
# ★ THE BARE `bpcvoter` IS DELIBERATELY NOT HERE — OWNER DECISION.
# `.env.blog.example` flags it: reputation 38.4 (positive) and it is bpcvoter1's on-chain
# `recovery_account`, "the one entry worth a second opinion before shipping". Not rendering
# it is reversible; excising it from the trust graph also zeroes its own graph-cred and its
# vouches for third parties. Add it here only if the owner confirms.
bpcvoter1
bpcvoter2
bpcvoter3
bpcvoter4

# --- owner call 2026-08-23: "remove ecency, qurator and worldmappin from algo
# ranker, they keep taking spots".
#
# These three were ONLY on curator_accounts.txt, which excludes an account's
# ENGAGEMENT and deliberately leaves their own posts ranking normally. That is why
# they kept taking feed slots: nothing was stopping their posts, only their votes.
# A ranker ban is the mechanism that stops the posts (`filter_eligible`).
#
# THIS IS THE RANKER LIST, NOT THE SITE-WIDE ONE. It removes them from ranked feeds
# and from every engagement signal. It does NOT hide them from Lumen: their profiles
# and their posts still load when linked to directly, because that is governed by the
# frontend's REACT_APP_BANNED_AUTHORS, which they are deliberately NOT on. They are
# service/curation accounts, not trolls.
#
# They were also removed from trusted_seeds.txt in the same change: an account whose
# edges are dropped in both directions cannot anchor TrustRank, and leaving them there
# would have kept handing them seed teleport mass they could not propagate.
ecency
qurator
worldmappin
